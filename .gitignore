# Julia
*.jl.cov
*.jl.*.cov
*.jl.mem
/Manifest.toml
/docs/build/
/docs/Manifest.toml
.DS_Store

# Renders write to /tmp, not the repo (except the committed showcase d5_accident.gif)
/tmp/

# Scrubbable video versions of the showcase — keep only the GIF in git
*.webm
*.mp4
